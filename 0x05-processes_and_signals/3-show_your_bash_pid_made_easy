#!/usr/bin/env bash
# This script displays the PID and process name of processes whose name contains the word 'bash'.

# Find processes and display PID and process name
pgrep -f bash | xargs -I{} echo "{} bash"
